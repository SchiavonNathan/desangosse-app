# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

---

# Sincronização Obrigatória (App Clone)
Este app mobile é o clone gêmeo de `cropbio-app/mobile`.
- Toda tela, componente, serviço ou correção feita aqui deve ser replicada em `cropbio-app/mobile` (e vice-versa).
- Não sobrescreva assets (`assets/`) ou configurações específicas de pacote no `app.json`.
